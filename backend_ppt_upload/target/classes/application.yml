server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pptdb?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.pptupload.model

jwt:
  secret: mySecretKey
  expire: 86400000   # 1天 (毫秒)

# 文件存储位置（相对于应用程序工作目录）
app:
  upload-dir: uploads
