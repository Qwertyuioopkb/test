<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.pptupload.mapper.UserMapper">

    <select id="findByUsername" parameterType="String" resultType="com.example.pptupload.entity.User">
        SELECT id, username, password, phone
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.example.pptupload.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, phone)
        VALUES(#{username}, #{password}, #{phone})
    </insert>

    <update id="updatePhone">
        UPDATE user
        SET phone = #{phone}
        WHERE id = #{id}
    </update>
</mapper>
